[[ -s "$HOME/.profile" ]] && source "$HOME/.profile"
# Load RVM int a shell session *as a function*
#[[ -s "/usr/local/rvm/scripts/rvm" ]] && source "/usr/local/rvm/scripts/rvm"
#[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm"
